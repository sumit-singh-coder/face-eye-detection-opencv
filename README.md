# Face & Eye Detection using OpenCV 👁️

This project uses Python and OpenCV to detect faces and eyes in real-time using a webcam.

## Features
- Real-time face detection
- Eye detection inside detected faces
- Uses Haar Cascade classifiers

## Requirements
- Python
- OpenCV (`pip install opencv-python`)

## How to Run
```bash
python main.py

Press S to exit.